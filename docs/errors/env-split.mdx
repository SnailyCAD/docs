# Cannot read properties of undefined (reading 'split')

This error could occur if a password/secret includes a `$`.

![Env split error message](/env-split.png)

## Possible ways to fix it

- Reset the password so it does **not** include a `$`
- Change the secret so it does **not** include a `$`
