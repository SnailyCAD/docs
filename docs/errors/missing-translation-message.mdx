# Missing message error

This error can occur if you're using custom translations that are outdated from the default `en`.

## Possible ways to fix it

- Update your custom translations with the default `en` one.
